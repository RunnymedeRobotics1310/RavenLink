// Package version exposes the RavenLink build version as a single
// const so the banner, dashboard footer, and any future build artifacts
// share one source of truth. Bump this for releases.
package version

// Version is the RavenLink semantic version string.
const Version = "1.0.0"
